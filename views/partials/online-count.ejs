<section class="side-card online-card" aria-live="polite">
  <span class="online-dot" aria-hidden="true"></span>
  <div><strong data-online-count><%= onlineCount %></strong><span data-online-label> connecté<%= onlineCount > 1 ? 's' : '' %> en ce moment</span></div>
</section>
